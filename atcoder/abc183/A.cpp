#include<bits/stdc++.h>
using namespace std;

int32_t main() {
  ios_base::sync_with_stdio(0);
  cin.tie(0);
  int n; cin >> n;
  if (n >= 0) cout << n << '\n';
  else cout << 0 << '\n';
  return 0;
}