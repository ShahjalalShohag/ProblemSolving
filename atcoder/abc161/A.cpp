#include<bits/stdc++.h>
using namespace std;

const int N = 3e5 + 9;

int32_t main()
{
    int a, b, c; cin >> a >> b >> c;
    swap(a, b); swap(a, c);
    cout << a << ' ' << b << ' ' << c << endl;
    return 0;
}
